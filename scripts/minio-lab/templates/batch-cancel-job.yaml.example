# batch job 取消请求体旧式说明。
# 当前 SDK 已对齐 MinIO madmin：取消 batch job 时使用 DELETE /cancel-job?id=<jobId>，
# jobId 来自 start-job 的 JSON 响应，因此 MINIO_LAB_BATCH_CANCEL_BODY_FILE 不再是必需项。
# 本文件仅用于人工排错或对照旧式 body 夹具时参考，不应提交真实 jobId。
jobID: lab-job-id
